# Tasks — naming-and-identity

## moc → index
- [x] git mv Catalog `<pillar>-moc.md → -index.md` (7) + mold `moc.md → index.md`; `type: moc → index`; headings; cross-links
- [x] frontmatter schema `moc → index`; vault-structure delta + live spec
- [x] `moc_links → index_links` (refine-execute, refine-prompt, examples/03, validate-scripts, agent-integration)
- [x] CONST-05 label; MOC→index terminology across specs + docs

## _effort → folder-note
- [x] scripts: rollover, refine-detect, kanban-render, reprospect (glob + link), close-daily (rule)
- [x] specs/schema: vault-structure (effort/spoil location), value-pipeline scenario, frontmatter
- [x] example Site `_effort.md → folder-note`; docs (method, obsidian, READMEs, examples)

## Record + gate
- [x] ADR-0013
- [x] regression green (openspec --strict, lints, validate-scripts 3.12/3.13, link-check, CI)
- [x] Gate 4 — Keith AUTHORIZE
- [ ] CHANGELOG [0.1.6]; archive; tag v0.1.6 + release
- [ ] live-vault migration (git mv + wikilink repoint + re-render)
